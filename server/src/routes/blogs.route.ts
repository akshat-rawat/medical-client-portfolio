const express = require("express");
const router = express.Router();

/* GET blogs */
router.get("/", "");

/* POST blogs */
router.post("/", "");

/* PUT blogs */
router.put("/:id", "");

/* DELETE blogs */
router.delete("/:id", "");

module.exports = router;
